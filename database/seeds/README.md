# Database Seeds

Place future PostgreSQL seed SQL files in this directory.

Seed scripts for this project will be written as plain SQL and run
manually with the pgAdmin Query Tool when seed data is requested.

Do not run seed scripts with `psql` for this project unless that
approach is explicitly requested later.
